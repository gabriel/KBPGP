platform :ios, "8.0"

pod "GHKit"
pod "ObjectiveSugar"
pod "NAChloride"
pod "TSTripleSec", :path => "../TSTripleSec/"
pod "Mantle"
pod "OpenSSL-gabriel", :path => "../OpenSSL-gabriel/"

target :Tests do
  pod "GRUnit", :path => "../GRUnit"
end
